/**
 * Tipos de parâmetros:
 * 
 * Query: Parâmetros nomeados passados na rota separado da rota por ?
 * Separado entre si por &
 * http://localhost:3333/users?name=Caio&page=3
 * const param = request.query;
 * console.log(param);
 * Resultado: { name: 'Caio', page: '3' }
 * 
 * Route: Parâmetros utilizados para identificar recursos como exemplo o id
 * app.get('/users/:id', (request, response) => {
 * http://localhost:3333/users/123528
 * const param = request.params;
 * console.log(param);
 * Resultado: { id: '123528' }
 * 
 * Request Body: Corpo da requisição usado para criar ou alterar recursos, json
 * Necessário dizer para o app que estamos usando JSON.
 * app.use(express.json());
 * Resultado: { nome: 'Caio Francolin', idade: '25' }
 * O resultado foi o valor passado no corpo da requisição em json
 */

 /**
  * Bancos de dados:
  * SQL: MySQL, **SQLite, PostgreSQL, Oracle, Microsoft SQL Server
  * NoSQL: MongoDB, CouchDB, etc
  * 
  * Driver: SELECT * FROM tabela
  * Query Builder: table('users').select('*').where()
  */

  Entidades
  ONG
  Caso (incident)

  Funcionalidades
  Login
  Logout
  Cadastro de ONG
  Cadastro de casos
  Deletar casos
  Listar casos apenas da ONG
  Listar todos os casos
  Entrar em contato por WA e email

  

